name: SIGAP CI/CD Pipeline

on:
  push:
    branches: [ "main", "master" ]
  pull_request:
    branches: [ "main", "master" ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Setup PHP
      uses: shivammathur/setup-php@v2
      with:
        php-version: '8.2'
        extensions: mbstring, dom, curl, libxml, mysql, zip

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'

    - name: Install Composer Dependencies
      run: composer install --no-ansi --no-interaction --no-scripts --no-progress --prefer-dist

    - name: Install NPM Dependencies & Build
      run: |
        npm install
        npm run build

    - name: Directory Permissions
      run: chmod -R 777 storage bootstrap/cache

    - name: Run PHP Checks
      run: php -l app/Models/*.php app/Http/Controllers/*.php

    # --- 🚀 DEPLOYMENT TEMPLATE (Uncomment and configure secrets to use) ---
    # - name: Deploy to Shared Hosting via FTP
    #   uses: SamKirkland/FTP-Deploy-Action@v4.3.5
    #   with:
    #     server: ${{ secrets.FTP_SERVER }}
    #     username: ${{ secrets.FTP_USERNAME }}
    #     password: ${{ secrets.FTP_PASSWORD }}
    #     local-dir: ./
    #     server-dir: /public_html/subdomain_sigap/
    #     exclude: |
    #       **/.git*
    #       **/node_modules/**
    #       **/.env
    #       **/sigap_backup_full_node/**
